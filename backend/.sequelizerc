const path = require('path');

module.exports = {
  config: path.resolve('src/config', 'config.json'), // your DB config
  'models-path': path.resolve('src/models'), // your models folder
  'seeders-path': path.resolve('src/seeders'), // optional
  'migrations-path': path.resolve('src/migrations'), // optional
};
